import pandas as pd

def forecast_expenses(data: pd.DataFrame, periods: int = 30) -> pd.DataFrame:
    """
    Simple stub for forecasting future expenses based on historical data.
    """
    # TODO: Implement actual forecasting logic (e.g., ARIMA, Prophet)
    print(f"Forecasting for {periods} periods...")
    return pd.DataFrame()
